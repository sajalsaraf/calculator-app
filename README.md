# Calculator App

A basic calculator built with plain HTML, CSS, and JavaScript.

## Run locally

From this folder, run:

```bash
python3 -m http.server 5500
```

Then open http://localhost:5500 in your browser.
